#lambda function no name and only used to return values

#4 parts to lambda function 1 - lambda keyword, 2- parameter list (x , y), 3 - colon 4 - return value
(lambda x,y: x + y)



def double(x):
    return x * 2
    return x * 2
sequence = [1,3,5,9]
#list comprehension usually faster than map
doubled = [double(x) for x in sequence]

doubled = map(double,sequence)



